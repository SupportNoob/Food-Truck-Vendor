# Food Truck Vendor

Food Truck Booking System for Alturas Capital Partners vendor scheduling.

This repository has been initialized so Codex/GitHub can index it. The current working HTML file is still available locally at:

`C:\Users\loop\Downloads\Food-Truck-Booking-System.html`

Recommended GitHub Pages setup once `index.html` is added:

1. Rename or add the app file as `index.html` at the repository root.
2. Open repository Settings.
3. Go to Pages.
4. Set source to `Deploy from a branch`.
5. Choose branch `main` and folder `/root`.
